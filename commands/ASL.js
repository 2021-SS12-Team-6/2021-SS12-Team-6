module.exports = function(message) {
    // open UDP channel

    // listen in on message channel


    // send each message to parser

    // process incoming image lists
    const parseOutput = "hello world.";

    // display image list
    let imageList;
    let topic, sign;
    let path = `./Assets/${topic}/${sign}`;

    // for (const image in imageList) {
    //     message.channel.send(image);
    // }
    
};