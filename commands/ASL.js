// 
module.exports = function (message) {
    // open UDP channel

    // listen in on message channel

    // send each message to parser

    // process incoming image lists

    // display image list
    let images = "ASL";
    message.channel.send(images);
};